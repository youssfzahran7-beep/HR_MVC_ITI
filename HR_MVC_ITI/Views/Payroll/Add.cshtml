@model HR_MVC_ITI.Models.ViewModels.PayrollViewModel

@{
    ViewData["Title"] = "Add Payroll";
}

<h1>Add Payroll</h1>

<form asp-action="SavePayroll" method="post" class="card card-body">
    @Html.AntiForgeryToken()

    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="mb-3">
        <label asp-for="EmployeeId" class="form-label">Employee Name</label>
        <select asp-for="EmployeeId" asp-items="ViewBag.Employees" class="form-select">
            <option value="">-- Select employee --</option>
        </select>
        <span asp-validation-for="EmployeeId" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Month" class="form-label"></label>
        <input asp-for="Month" class="form-control" />
        <span asp-validation-for="Month" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Year" class="form-label"></label>
        <input asp-for="Year" class="form-control" />
        <span asp-validation-for="Year" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="BasicSalary" class="form-label">Basic Salary (Auto-Calculated on Save)</label>
        <input asp-for="BasicSalary" class="form-control" readonly value="0" />
    </div>

    <div class="mb-3">
        <label asp-for="LateDeductions" class="form-label">Late Deductions (Auto-Calculated on Save)</label>
        <input asp-for="LateDeductions" class="form-control" readonly value="0" />
    </div>

    <div class="mb-3">
        <label asp-for="OvertimeAdditions" class="form-label">Overtime Additions (Auto-Calculated on Save)</label>
        <input asp-for="OvertimeAdditions" class="form-control" readonly value="0" />
    </div>

    <button type="submit" class="btn btn-success">Save</button>
    <a asp-action="GetAll" class="btn btn-secondary">Back to table</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
